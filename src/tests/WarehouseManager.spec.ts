import WarehouseManager from "../WarehouseManager"

describe(WarehouseManager.name, () => {
  beforeEach(() => {
    // TODO create warehouseManager obj
    // TODO mock Client
    // TODO mock order.process
  })
  test("fetchDataFromServer populates object properties", () => {})
  test("processOrders errors without data", () => {})
  test("processOrders processes every order", () => {})
  test("processOrders generates report", () => {})
})
