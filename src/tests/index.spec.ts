import processOrdersAndLogReports from ".."

describe(processOrdersAndLogReports.name, () => {
  beforeEach(() => {
    // TODO mock WarehouseManager
  })
  it("fetches data from the server", () => {})
  it("logs the process report", () => {})
  it("logs all the invoices", () => {})
  it("logs the restock report", () => {})
})
