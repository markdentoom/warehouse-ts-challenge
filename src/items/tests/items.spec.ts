import { GenericItem } from "../items"

describe(GenericItem.name, () => {
  beforeEach(() => {
    // TODO create instance of abstract class?
  })
  test("decreaseStock with delta 0", () => {})
  test("decreaseStock decreases stock", () => {})
  test("decreaseStock errors with negative delta", () => {})
  test("decreaseStock errors with delta greater than stock", () => {})
})
